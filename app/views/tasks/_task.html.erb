<div id="<%= dom_id task %>">
  <p>
    <strong>Name:</strong>
    <%= task.name %>
  </p>

  <p>
    <strong>Due date:</strong>
    <%= "#{task.year}-#{task.month}-#{task.day}" %>
  </p>

  <p>
    <strong>Complete:</strong>
    <%= task.complete %>
  </p>
</div>
